# Salin file ini ke .env lalu sesuaikan nilainya
# cp .env.example .env

# Port lokal (hanya diakses tunnel, jangan buka ke internet)
PORT=3000

# Bind ke localhost saja — aman untuk tunnel
HOST=127.0.0.1

# Environment
NODE_ENV=production
